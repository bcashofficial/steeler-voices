# The rig's only credential. Copy to ~/.config/steeler-voices/rig.env (or export it).
# A RunPod account: https://www.runpod.io — Settings → API Keys.
# This is the one credential in the platform, and only for the optional rig;
# `make dev` needs none.
RUNPOD_API_KEY=
